/// @description scr_orbit_create2(speed,starting dir,radius,point to orbit around);
/// @param speed
/// @param starting dir
/// @param radius
/// @param point to orbit around

orbit_speed = argument0;    //Bigger radius -> smaller speed
orbit_dir = argument1;      //Starting point of orbit
orbit_rad = argument2;      //Radius
orbitx = argument3.x;       //Point to orbit around
orbity = argument3.y;
