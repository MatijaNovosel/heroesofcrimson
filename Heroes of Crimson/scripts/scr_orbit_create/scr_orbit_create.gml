/// @description scr_orbit_create(starting angle,radius,speed);
/// @param starting angle
/// @param radius
/// @param speed
orbit_angle = argument0;
orbit_radius = argument1;
orbit_speed = argument2 * (360 / room_speed); //360 = 1 orbit per second
