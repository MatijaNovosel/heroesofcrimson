// Visual
barrier_color = 0;

// Wave motion
t = 0;
increment = degtorad(3); // 12 degrees, now converted to radians -- freq = 1 oscillation per second (1Hz)
amplitude = 0.25; // pixels of peak oscillation

// Image scaling
scale = 1;
scale += 0.25;
image_xscale = scale;
image_yscale = scale;